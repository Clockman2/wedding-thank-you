# Assets

This folder contains site-ready public images.

- `rings-bible-bg.jpg` is used by the hero section.
- `story-figurines-bg.jpg` is used by the story section.
- `guests/` contains optional guest splash photos.

For a public demo repository, keep these images generic and remove private EXIF metadata before committing.
